# AWS Trainium Chess Workshop - Environment Configuration
# Copy this file to .env and customize as needed
# All settings are optional; defaults work for standard workshop environment

# ============================================================
# vLLM Server Configuration (Your Fine-Tuned Model)
# ============================================================
# These settings connect to your locally deployed vLLM server
# See vllm-server/README.md for deployment instructions

VLLM_BASE_URL=http://localhost:8000/v1
VLLM_MODEL=Qwen3-chess
VLLM_TEMPERATURE=0.1
VLLM_MAX_TOKENS=50
VLLM_TIMEOUT=30.0

# ============================================================
# Stockfish Configuration (Baseline Opponent)
# ============================================================
# Path to Stockfish binary (only needed if not in system PATH)
# Default: auto-detected from PATH
# Uncomment and set if Stockfish is in a custom location

# STOCKFISH_PATH=/usr/local/bin/stockfish
# STOCKFISH_PATH=/opt/homebrew/bin/stockfish  # macOS Homebrew
# STOCKFISH_PATH=C:\Program Files\Stockfish\stockfish.exe  # Windows

# ============================================================
# Chess Environment Settings
# ============================================================
# Global game parameters (can be overridden via CLI arguments)

CHESS_MAX_MOVES=100        # Max moves per game before draw
CHESS_TIME_LIMIT=30.0      # Time limit per move (seconds)

# ============================================================
# Workshop Notes
# ============================================================
#
# Quick Start:
# 1. Copy this file: cp env.example .env
# 2. Deploy vLLM: cd vllm-server && bash compile.sh && bash vllm.sh
# 3. Test connection: python -c "from agents import VLLMAgent; print(VLLMAgent().test_connection())"
# 4. Run tournament: python run_game.py --agent vllm --agent stockfish-skill5-depth10 --num-games 10
#
# For detailed instructions, see README.md
